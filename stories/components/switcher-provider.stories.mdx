import { Meta, Props } from '@storybook/addon-docs/blocks';

import { SwitcherProvider } from 'provider';

<Meta title="Components/SwitcherProvider" />

# SwitcherProvider

A Provider component, that provides `<LanguageContext />` to its children.

It allows any child, regardless of the depth, to have access to the context value object based on LanguageContextProps

## Element

```tsx
<SwitcherProvider />
```

## Usage

```tsx
<SwitcherProvider>{children}</SwitcherProvider>
```

## Props

<Props of={SwitcherProvider} />
