import styled from 'styled-components';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';

import { LanguageSwitcher } from 'middleware/language';
import { SwitcherProvider } from 'provider';

<Meta title="Examples/styling/styled-components" />

export const fontFamily =
  "'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', 'Geneva', 'Verdana', 'sans-serif'";

export const Container = styled.div`
  background-color: #25aaff;
`;

export const List = styled.ul`
  font-family: ${fontFamily};
  display: flex;
  list-style: none;
  padding: 0;
`;

export const Item = styled.li`
  margin: 0 8px;
`;

export const borderBottom = '3px solid white';
export const Button = styled.button.attrs(props => ({
  isActive: props['data-active'],
}))`
  color: white;
  text-transform: uppercase;
  padding: 24px;
  display: inline-block;
  vertical-align: middle;
  touch-action: manipulation;
  user-select: none;
  cursor: pointer;
  white-space: nowrap;
  text-decoration: none;
  text-align: center;
  border: 0;
  background: transparent;
  transition: all 0.15s ease-in-out;
  font-size: 12px;
  font-family: ${fontFamily};
  text-transform: uppercase;
  letter-spacing: 0.2em;
  border-bottom: ${props => (props.isActive ? borderBottom : 'none')};
  &:hover {
    border-bottom: ${borderBottom};
  }
  &:active {
    border-bottom: ${borderBottom};
  }
`;

# Styled Components

Styling with Styled Components

## Preview

<Preview withSource="none">
  <SwitcherProvider>
    <LanguageSwitcher
      languageList={['en', 'de', 'fr']}
      containerComponent={Container}
      listComponent={List}
      itemComponent={Item}
      buttonComponent={Button}
    />
  </SwitcherProvider>
</Preview>

## Usage

```tsx
<SwitcherProvider>
  <LanguageSwitcher
    languageList={['en', 'de', 'fr']}
    containerComponent={Container}
    listComponent={List}
    itemComponent={Item}
    buttonComponent={Button}
  />
</SwitcherProvider>
```

## Props

<Props
  of={LanguageSwitcher}
  exclude={[
    'containerClassName',
    'listClassName',
    'itemClassName',
    'buttonClassName',
    'buttonActiveClassName',
  ]}
/>
