import { StyleSheet, css } from 'aphrodite/no-important';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';

import { Switcher, LanguageProvider } from 'components';

export const borderBottom = '3px solid white';
export const fontFamily = "'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#25aaff',
  },
  list: {
    fontFamily,
    display: 'flex',
    listStyle: 'none',
    padding: 0,
  },
  item: {
    margin: '0 8px',
  },
  button: {
    color: 'white',
    textTransform: 'uppercase',
    padding: '24px',
    display: 'inline-block',
    verticalAlign: 'middle',
    touchAction: 'manipulation',
    userSelect: 'none',
    cursor: 'pointer',
    whiteSpace: 'nowrap',
    textDecoration: 'none',
    textAlign: 'center',
    border: 0,
    background: 'transparent',
    transition: 'all 0.15s ease-in-out',
    fontSize: '12px',
    letterSpacing: '0.2em',
    fontFamily,
    ':hover': { borderBottom },
    ':active': { borderBottom },
  },
  active: { borderBottom },
});

<Meta title="Examples/styling/aphrodite" />

# Aphrodite

Styling with Aphrodite

## Preview

<Preview withSource="none">
  <LanguageProvider config={{ languageList: ['en', 'de', 'fr']}}>
    <Switcher
      containerClassName={css(styles.container)}
      listClassName={css(styles.list)}
      itemClassName={css(styles.item)}
      buttonClassName={css(styles.button)}
      buttonActiveClassName={css(styles.active)}
    />
  </LanguageProvider>
</Preview>

## Usage

```tsx
<LanguageProvider config={{ languageList: ['en', 'de', 'fr']}}>
  <Switcher
    containerClassName={css(styles.container)}
    listClassName={css(styles.list)}
    itemClassName={css(styles.item)}
    buttonClassName={css(styles.button)}
    buttonActiveClassName={css(styles.active)}
  />
</LanguageProvider>
```

## Props

<Props
  of={Switcher}
  exclude={[
    'containerComponent',
    'listComponent',
    'itemComponent',
    'buttonComponent',
  ]}
/>
